<UserControl x:Class="Mango.Setup.Views.SetupHomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

    <Grid x:Name="LayoutRoot" Background="White">

        <Border CornerRadius="13" BorderThickness="0,0,1,1" BorderBrush="Gray" Background="WhiteSmoke" Margin="10,10,0,10" >
            <Border.Effect>
                <DropShadowEffect Color="DimGray"></DropShadowEffect>
            </Border.Effect>

            <StackPanel Margin="20">
                <TextBlock FontSize="15" FontWeight="Bold" FontFamily="Verdana">Welcome to Setup Module</TextBlock>
                <TextBlock  FontSize="12" TextWrapping="Wrap">With the Stock feature, you can set up new, modify or remove the details of stock items including stock categories, stock types, stock names, package types, package re order level, package type quantity, stock price, stock return type and stock state; managing purchase stock (recording stock, registering stock and modifying registered stock); arranging stock on shelf, modifying stock on shelf and removing stock on shelf; selling stock and also, modifying sold stock.</TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
